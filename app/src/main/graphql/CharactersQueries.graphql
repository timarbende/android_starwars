query AllCharactersQuery {
    allPeople {
        people {
            name
            id
            filmConnection {
                totalCount
            }
        }
    }
}

query Person($personId: ID) {
    person(id: $personId) {
        id
        birthYear
        eyeColor
        gender
        hairColor
        height
        mass
        name
    }
}